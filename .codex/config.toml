# Generated and owned by Harness. Do not edit this file in place.
# After this file changes, fully restart Codex and begin a new task.
developer_instructions = "Harness is required for repository work. `project_status` must be the first repository action at the start of every Codex task. Before running any shell command, reading or searching repository files, inspecting with a browser, or making changes, locate the configured Harness MCP tools\u2014even when they are deferred or omitted from the initial visible tool list\u2014and call `project_status`. Tool discovery is the only action allowed before `project_status`; do not treat initial omission as unavailability. After Harness config changes, use a fully restarted Codex client and new task; existing instruction snapshots do not refresh.\n\nAfter successful `project_status`, start or resume a Harness Task before diagnosis or edits; on schema error retry, never skip. Do not skip Task because work looks small or the path is known. Then use `project_search` before broad native exploration; an already-known exact path may skip search, not Task; a code/doc path may be read natively. Checkpoint each logical stage.\n"

[mcp_servers.harness]
url = "http://127.0.0.1:17375/mcp"
required = true
startup_timeout_sec = 30

[mcp_servers.harness.http_headers]
Authorization = "Bearer a-9ZwMELFL7FoCAd03trlt8JdzTgHLmYlL0lFVt5NBA"
"X-Harness-Workspace-Root" = "/home/gorills/projects/sim"

[mcp_servers.blender]
command = "/home/gorills/.local/bin/blender-mcp"
args = []
startup_timeout_sec = 30

[mcp_servers.blender.env]
BLENDER_PATH = "/home/gorills/.local/opt/blender-5.2.0-linux-x64/blender"
BLENDER_MCP_HOST = "localhost"
BLENDER_MCP_PORT = "9876"
